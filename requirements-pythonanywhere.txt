# Lean dependency set for PythonAnywhere free tier.
# Streamlit/pandas are NOT needed for the WSGI web UI served via pythonanywhere_wsgi.py.
# Browser/stealth modes need Playwright browsers which PythonAnywhere free tier
# cannot run; HTTP mode (curl_cffi) works fine.
scrapling[fetchers]>=0.4.11,<0.5
click>=8.0,<9
httpx>=0.27,<1
python-multipart>=0.0.9,<1
platformdirs>=4.0,<5
pydantic>=2.0,<3
markdownify>=0.11,<2
